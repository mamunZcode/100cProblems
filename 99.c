#include <math.h>
#include <stdio.h>

int main()
{
    int number;
    double result;
    scanf("%d",&number);

    result = sqrt(number);
    int final_result=result+.5;
    printf("Rsult is: %d",final_result);


return 0;
}
